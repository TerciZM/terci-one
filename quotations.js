export async function onRequestGet({ env }) {
  const result = await env.DB.prepare('SELECT q.id,q.quote_number,q.quote_date,q.validity_days,q.status,q.total,c.name AS customer_name FROM quotations q LEFT JOIN customers c ON c.id=q.customer_id ORDER BY q.created_at DESC LIMIT 10').all();
  return Response.json(result.results || []);
}
export async function onRequestPost({ env, request }) {
  try { const b=await request.json(); const lines=Array.isArray(b.lines)?b.lines:[], costs=Array.isArray(b.additional_costs)?b.additional_costs:[]; if(!b.quote_number||!b.quote_date) return Response.json({error:'Quote number and date are required'},{status:400}); const subtotal=lines.reduce((s,l)=>s+Number(l.quantity||1)*Number(l.selling_price||0),0), additional=costs.reduce((s,c)=>s+Number(c.amount||0),0), profit=lines.reduce((s,l)=>s+Number(l.quantity||1)*(Number(l.selling_price||0)-Number(l.internal_cost||0)),0), total=subtotal+additional; const r=await env.DB.prepare('INSERT INTO quotations (quote_number,customer_id,quote_date,validity_days,status,subtotal,additional_costs,profit,total) VALUES (?,?,?,?,\'Draft\',?,?,?,?)').bind(b.quote_number,b.customer_id||null,b.quote_date,Number(b.validity_days||30),subtotal,additional,profit,total).run(); const id=r.meta.last_row_id; for(const l of lines) await env.DB.prepare('INSERT INTO quotation_lines (quotation_id,item_id,description,quantity,selling_price,internal_cost,profit) VALUES (?,?,?,?,?,?,?)').bind(id,l.item_id||null,l.description||'Item',Number(l.quantity||1),Number(l.selling_price||0),Number(l.internal_cost||0),Number(l.quantity||1)*(Number(l.selling_price||0)-Number(l.internal_cost||0))).run(); for(const c of costs) await env.DB.prepare('INSERT INTO quotation_additional_costs (quotation_id,description,amount) VALUES (?,?,?)').bind(id,c.description||'Additional cost',Number(c.amount||0)).run(); return Response.json({id,total},{status:201}); } catch(e) { return Response.json({error:e.message},{status:500}); }
}
