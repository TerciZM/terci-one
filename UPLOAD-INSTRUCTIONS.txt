Replace app.js and functions/api/quotations.js in the matching locations in your GitHub repository. Keep terci-logo.png in the website root. Wait for Cloudflare deployment, then hard refresh. No database migration or seed import is required. The HTML preview uses the application template with sample data. The PDF is a layout mockup; browser pagination may differ. Use Print / Save PDF in the application to export your actual quotation.
