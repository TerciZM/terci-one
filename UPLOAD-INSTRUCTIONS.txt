TERCI QUOTATION UI UPDATE

This update adds the Terci logo as the browser-tab icon, makes the item/description search field a consistent styled input, and includes the corrected quotation save endpoint.

Replace the matching files in the root of the GitHub Pages project with the files in this ZIP, preserving the functions/api/quotations.js path. Commit the changes and let Cloudflare Pages finish deploying.

No database SQL is required. The subject column is already present.
